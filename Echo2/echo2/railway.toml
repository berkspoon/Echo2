[build]
builder = "nixpacks"

[build.nixpacks]
providers = ["python"]
buildCmd = "python -m pip install -r requirements.txt"

[deploy]
startCommand = "python -m uvicorn app.main:app --host 0.0.0.0 --port ${PORT}"
```